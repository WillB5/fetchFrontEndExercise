import { useState } from "react";

function Search() {
  const [count, setCount] = useState(0);

  return (
    <>
      <title>Search</title>
      <h1>Search Page</h1>
    </>
  );
}

export default Search;
